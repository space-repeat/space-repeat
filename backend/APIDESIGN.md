## API Structure

🔑 indicates that the particular route need to be authorized

### Stacks
GET `/api/stacks` 🔑

returns all stacks for a particular user.

POST `/api/stacks` 🔑

create a new stack

PUT `/api/stacks/{id}` 🔑

update a stack
